package org.fao.test;

/**
 * To test the whole system functionality, as opposed to unitary tests, that
 * does not require Geoserver, GeoStore, etc. installation (Only the class being
 * tested typically)
 * 
 * @author fergonco
 */
public interface FunctionalTestSuite {

}
